import { Still } from "remotion";
import { Poster } from "./Poster";

export const Composition: React.FC = () => {
  return (
    <Still
      id="AniShelf-Overview-Poster-ZH"
      component={Poster}
      width={1920}
      height={1080}
    />
  );
};
